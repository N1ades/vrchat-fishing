# GitHub Actions для VRChat Fishing Bot

Этот репозиторий использует GitHub Actions для автоматической сборки и создания релизов.

## 📋 Доступные Workflows

### 1. 🔄 Build on Push (`build-on-push.yml`)
**Назначение**: Автоматическая сборка при каждом коммите

**Триггеры**:
- Push в ветки `main`, `master`, `develop`
- Pull Request в `main`, `master`

**Что делает**:
- Устанавливает зависимости
- Собирает EXE файл
- Проверяет успешность сборки
- Сохраняет артефакт на 7 дней

**Как использовать**:
```bash
git add .
git commit -m "Ваши изменения"
git push
```

Результат можно найти в разделе "Actions" → Скачать артефакт

---

### 2. 🎉 Build and Release (`build-release.yml`)
**Назначение**: Автоматическое создание релиза при создании тега

**Триггеры**:
- Создание тега версии (формат: `v*.*.*`)
- Ручной запуск через "Actions"

**Что делает**:
- Собирает EXE файл
- Упаковывает все необходимые файлы
- Создает публичный релиз на GitHub
- Прикрепляет файлы к релизу

**Как использовать**:
```bash
# Создайте тег
git tag v1.0.0

# Отправьте тег на GitHub
git push origin v1.0.0
```

Релиз появится в разделе "Releases" автоматически.

---

### 3. 📝 Create Draft Release (`create-draft-release.yml`)
**Назначение**: Создание черновика релиза с возможностью редактирования

**Триггеры**:
- Только ручной запуск

**Что делает**:
- Собирает EXE файл
- Создает ZIP архив
- Создает черновик релиза
- Позволяет отредактировать перед публикацией

**Как использовать**:
1. Откройте "Actions" на GitHub
2. Выберите "Create Draft Release"
3. Нажмите "Run workflow"
4. Введите версию (например: `1.0.0`)
5. Укажите, является ли это пре-релизом
6. Запустите workflow
7. После сборки отредактируйте черновик в разделе "Releases"
8. Опубликуйте когда готово

---

## 🚀 Быстрый старт

### Первый релиз:
```bash
# 1. Убедитесь что всё работает
git add .
git commit -m "Готов к первому релизу"
git push

# 2. Создайте тег первой версии
git tag v1.0.0
git push origin v1.0.0

# 3. Дождитесь окончания сборки (~5 минут)
# 4. Проверьте раздел Releases
```

### Обновление версии:
```bash
# 1. Внесите изменения
git add .
git commit -m "Описание изменений"
git push

# 2. Создайте новый тег
git tag v1.1.0
git push origin v1.1.0
```

### Бета-версия:
```bash
# Используйте суффикс для бета-версий
git tag v1.1.0-beta.1
git push origin v1.1.0-beta.1
```

Или используйте "Create Draft Release" с галочкой "Pre-release".

---

## 📦 Что включается в релиз

Каждый релиз содержит:
- ✅ `VRChat_Fishing_Bot.exe` - исполняемый файл
- ✅ `fishing_bot_settings.json` - настройки
- ✅ `README.md` - документация
- ✅ `QUICK_START.txt` - быстрый старт
- ✅ ZIP архив со всеми файлами (только в draft release)

---

## 🔍 Мониторинг сборки

### Проверить статус:
1. Откройте репозиторий на GitHub
2. Перейдите в раздел "Actions"
3. Найдите ваш workflow
4. Проверьте статус:
   - ✅ Зелёная галочка = успешно
   - ❌ Красный крестик = ошибка
   - 🟡 Жёлтый кружок = в процессе

### Просмотр логов:
1. Кликните на workflow
2. Кликните на job "build"
3. Раскройте нужный шаг
4. Просмотрите детальные логи

---

## ⚙️ Конфигурация

### Изменить версию Python:
Отредактируйте в workflow файле:
```yaml
- name: Set up Python
  uses: actions/setup-python@v5
  with:
    python-version: '3.11'  # Измените здесь
```

### Добавить файлы в релиз:
Отредактируйте секцию в `build-release.yml`:
```yaml
- name: Create Release Package
  run: |
    mkdir release
    copy dist\VRChat_Fishing_Bot.exe release\
    copy fishing_bot_settings.json release\
    copy ВАШ_НОВЫЙ_ФАЙЛ.txt release\  # Добавьте здесь
```

### Изменить триггеры:
Отредактируйте секцию `on:` в начале workflow файла.

---

## 🐛 Решение проблем

### Ошибка: "pyinstaller not found"
**Причина**: PyInstaller не установился
**Решение**: Проверьте логи установки зависимостей

### Ошибка: "Module not found"
**Причина**: Не хватает зависимости в requirements.txt
**Решение**: Добавьте зависимость в requirements.txt

### Релиз не создался
**Причина**: Возможно неправильный формат тега
**Решение**: Используйте формат `v1.0.0` (с буквой v)

### Сборка слишком долгая
**Причина**: Нормально, первая сборка может занять до 10 минут
**Решение**: Последующие сборки будут быстрее благодаря кэшированию

---

## 📊 Статистика

Примерное время выполнения:
- 📦 Установка зависимостей: ~1-2 минуты
- 🔨 Сборка EXE: ~2-4 минуты
- 📤 Создание релиза: ~30 секунд
- **Всего**: ~4-7 минут

Размер артефактов:
- EXE файл: ~40-50 МБ
- ZIP архив: ~35-45 МБ (сжатый)

---

## 📚 Дополнительная информация

- [Руководство по созданию релизов](.github/RELEASE_GUIDE.md)
- [GitHub Actions Docs](https://docs.github.com/en/actions)
- [PyInstaller Docs](https://pyinstaller.org/)

---

## 💡 Советы

1. **Тестируйте локально** перед созданием релиза
2. **Используйте семантическое версионирование** (v1.0.0)
3. **Пишите подробный changelog** в описании релиза
4. **Используйте draft releases** для проверки перед публикацией
5. **Проверяйте артефакты** перед финальной публикацией

---

## 🤝 Вклад в проект

При внесении изменений:
1. Создайте feature ветку
2. Сделайте изменения
3. Создайте Pull Request
4. Дождитесь автоматической сборки
5. После мерджа создайте новый релиз
