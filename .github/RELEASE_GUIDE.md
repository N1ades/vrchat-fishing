# Руководство по созданию релизов

## Автоматическая сборка и публикация релизов

### 🔄 Два типа сборок:

#### 1. Сборка при каждом коммите (build-on-push.yml)
- **Когда**: При каждом push в main/master/develop
- **Что делает**: 
  - Собирает EXE файл
  - Проверяет успешность сборки
  - Сохраняет артефакт на 7 дней
- **Результат**: Можно скачать артефакт из вкладки "Actions"

#### 2. Создание релиза (build-release.yml)
- **Когда**: При создании тега версии (например, v1.0.0)
- **Что делает**:
  - Собирает EXE файл
  - Создает пакет со всеми необходимыми файлами
  - Публикует официальный релиз на GitHub
- **Результат**: Публичный релиз в разделе "Releases"

## 📝 Как создать релиз:

### Способ 1: Через командную строку (рекомендуется)

```bash
# 1. Убедитесь, что все изменения закоммичены
git add .
git commit -m "Подготовка к релизу v1.0.0"
git push

# 2. Создайте тег с версией
git tag v1.0.0

# 3. Отправьте тег на GitHub
git push origin v1.0.0
```

### Способ 2: Через интерфейс GitHub

1. Откройте репозиторий на GitHub
2. Перейдите в "Releases" → "Create a new release"
3. Нажмите "Choose a tag"
4. Введите новый тег (например, `v1.0.0`) и нажмите "Create new tag"
5. Заполните название и описание релиза
6. Нажмите "Publish release"

### Способ 3: Ручной запуск

1. Откройте репозиторий на GitHub
2. Перейдите в "Actions"
3. Выберите "Build and Release"
4. Нажмите "Run workflow"
5. Выберите ветку и запустите

## 📋 Формат версий (Semantic Versioning)

Используйте формат: `vMAJOR.MINOR.PATCH`

- **MAJOR** (1.0.0): Несовместимые изменения API
- **MINOR** (0.1.0): Новая функциональность (обратно совместимая)
- **PATCH** (0.0.1): Исправление багов

Примеры:
- `v1.0.0` - Первый стабильный релиз
- `v1.1.0` - Добавлена новая функция
- `v1.1.1` - Исправлен баг
- `v2.0.0` - Крупные изменения

## 🔍 Проверка статуса сборки

1. Откройте репозиторий на GitHub
2. Перейдите в "Actions"
3. Найдите свой workflow
4. Проверьте статус (✓ успешно, ✗ ошибка)

## 📦 Что включается в релиз:

- `VRChat_Fishing_Bot.exe` - Основной исполняемый файл
- `fishing_bot_settings.json` - Файл настроек
- `README.md` - Документация
- `QUICK_START.txt` - Быстрый старт

## ⚠️ Важные замечания:

1. **Тег должен начинаться с `v`**: v1.0.0 ✓, 1.0.0 ✗
2. **Не удаляйте теги после создания** (можно создать новый)
3. **Проверяйте код перед созданием тега**
4. **Сборка занимает 3-5 минут**

## 🐛 Решение проблем:

### Сборка не запустилась
- Проверьте формат тега (должен быть `v*.*.*`)
- Убедитесь, что тег отправлен на GitHub (`git push origin v1.0.0`)

### Ошибка при сборке
- Откройте логи в "Actions"
- Проверьте, что все файлы на месте
- Убедитесь, что код компилируется локально

### Релиз не создался
- Проверьте права доступа (GITHUB_TOKEN должен иметь права на создание релизов)
- Убедитесь, что workflow завершился успешно

## 📊 Пример workflow:

```
Разработка → Коммит → Push → Автоматическая сборка → Тест
                                    ↓
                              Артефакт сохранен
                                    
Создание тега → Push тега → Автоматическая сборка → Релиз
                                    ↓
                              Публичный релиз
```

## 🎯 Рекомендации:

1. **Часто коммитьте** - сборка проверит код
2. **Тестируйте локально** перед созданием релиза
3. **Пишите changelog** в описании релиза
4. **Используйте pre-release** для бета-версий
5. **Следуйте Semantic Versioning**

## 📝 Шаблон changelog для релиза:

```markdown
## Что нового в v1.1.0

### ✨ Новые функции:
- Добавлена поддержка X
- Улучшена функция Y

### 🐛 Исправления:
- Исправлен баг с Z
- Улучшена стабильность

### 📚 Документация:
- Обновлена инструкция
- Добавлены примеры

### ⚠️ Известные проблемы:
- Проблема A (в работе)
```

## 🔗 Полезные ссылки:

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Semantic Versioning](https://semver.org/)
- [PyInstaller Manual](https://pyinstaller.org/en/stable/)
